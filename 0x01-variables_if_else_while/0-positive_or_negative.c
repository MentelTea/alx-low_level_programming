#include <stdlib.h>
#include <time.h>
#include <stdlio.h>

int main(void)

{
		int n;

			srand(time(0));
				n = rand() - RAND_MAX / 2;
			if (n < 0)
			{
			printf("is positive")
			if else(n == 0)
			printf("is zero")
			if else(n > 0)
			Printf("is negative")
	return (0);
}
